export const assertEquals = (actual, expected) => actual === expected;

export const assertFalse = (actual, expected) => actual === false;
export const assertTrue = (actual, expected) => actual === true;
